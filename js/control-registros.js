import * as usuarios from "../modulos/usuarios.js"

const inputFechaDeInicio = document.getElementById("inputFechaInicio")
const inputFechaFinal = document.getElementById("inputFechaFinal")
const inputFiltroRegistroAdmin = document.getElementById("filtroRegistroAdmin")

inputFiltroRegistroAdmin.addEventListener("click", function(e) {
    e.preventDefault()
    //borro todo de la tabla

    //traigo el nombreDeUsuario elegido
    //traigo el mapaDeCarritos asociado 
    //recorro el mapa de carritos, chequeando cada carrito
    //filtro la fecha de los carritos(si cumple las condiciones)
    //lo muestro con la targeta ya hecha en js
    // la targete tiene que tener, información de usuario, fecha, productos, cantidades y monto total
    //(muestro el carrito, y dentro las compras)

    
})
